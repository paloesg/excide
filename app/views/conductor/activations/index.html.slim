.row
  .col-sm-12
    ol.breadcrumb
      li.breadcrumb-item.ml-auto
        a href="/conductor" Dashboard
      li.breadcrumb-item
        a href="/symphony/company" #{@company.name}
      li.breadcrumb-item.active Activations
    h1
      | Activations Listing - #{@date_from.strftime("%B")}
      = link_to 'Activation Types', conductor_activation_types_path, role: 'button', class: 'btn btn-success float-right mr-1' if @user.has_role? :admin, :any
      = link_to 'New Activation', new_conductor_activation_path, role: 'button', class: 'btn btn-warning float-right mr-1' unless @user.has_role? :associate, :any
.row
  .col-sm-12.mb-3
    .btn-group.float-right role='group'
      = link_to "?start_date=#{params[:start_date].present? ? Date.parse(params[:start_date]).beginning_of_month.prev_month : Date.current.beginning_of_month.prev_month}", type: 'button', class: 'btn btn-sm'
        i.fa.fa-chevron-left
        | Previous Month
      = link_to "?start_date=#{Date.current.beginning_of_month}", type: 'button', class: 'btn btn-sm'
        | Current Month
      = link_to "?start_date=#{params[:start_date].present? ? Date.parse(params[:start_date]).beginning_of_month.next_month : Date.current.beginning_of_month.next_month}", type: 'button', class: 'btn btn-sm'
        | Next Month
        i.fa.fa-chevron-right
.row
  .col-sm-12
    .table-responsive
      table.table.table-hover
        thead
          tr
            th
            th Activation type
            th Client
            th Staffer
            th Start time
            th End time
            th Location
            - if @user.has_role? :associate, :any
              th Remarks
            - else
              th Consultant
              th Associate
              th
              th
        tbody
          - @activations.each do |activation|
            tr
              td.text-center
                i.fa.fa-circle.mr-1 style="color: #{activation.activation_type.colour};"
              td = activation.activation_type.name
              td = activation.client&.name
              td = activation.staffer&.full_name
              td = activation.start_time.strftime("%d/%m/%y %H:%M")
              td = activation.end_time.strftime("%d/%m/%y %H:%M")
              td = activation.location
              - if @user.has_role? :associate, :any
                td = activation.remarks
              - else
                td
                  - if activation.allocations.consultant.empty? and ((@user.has_role? :manpower_allocator, @company) or @user.has_role? :admin, @company)
                    = select_tag('allocations', options_for_select((activation.allocations.consultant.count..5).to_a, activation.allocations.consultant.count), onchange: "create_allocations(#{activation.id}, 'consultant', this);")
                  - else
                    = select_tag('allocations', options_for_select([activation.allocations.consultant.count]), disabled: true)
                td
                  - if activation.allocations.associate.empty? and ((@user.has_role? :manpower_allocator, @company) or @user.has_role? :admin, @company)
                    = select_tag('allocations', options_for_select((activation.allocations.associate.count..20).to_a, activation.allocations.associate.count), onchange: "create_allocations(#{activation.id}, 'associate', this);")
                  - else
                    = select_tag('allocations', options_for_select([activation.allocations.associate.count]), disabled: true)
                td
                  i.fa.fa-check.text-success style="opacity: 0;"
                  i.fa.fa-times.text-danger style="opacity: 0;"
                td.btn-toolbar
                  = link_to 'Details', '#', role: 'button', class: 'btn btn-success btn-sm mr-1 mt-1 activation', 'activation-data': "#{activation.to_json}", 'activation-address': "#{activation.address.to_json}", 'activation-client': "#{activation.client&.name}", 'activation-staffer': "#{activation.staffer&.full_name}", get_allocated_users_length: "#{activation.allocations.map(&:user).reject(&:blank?).length}", title: "Activation Details <div class='float-right'><span class='popover-close fa fa-times' aria-hidden='true'></span></div>", 'data-placement': "left", 'activation-type': "#{activation.activation_type.name}", data: { turbolinks: false }
                  = link_to 'Edit', edit_conductor_activation_path(activation), role: 'button', class: 'btn btn-primary btn-sm mr-1 mt-1'
                  = link_to 'Reset', reset_conductor_activation_path(activation), data: { confirm: 'This will remove all existing allocations for this activation. Are you sure?' }, method: :post, role: 'button', class: 'btn btn-warning btn-sm mr-1 mt-1'
                  = link_to 'Delete', conductor_activation_path(activation), data: { confirm: 'Are you sure?' }, method: :delete, role: 'button', class: 'btn btn-danger btn-sm mt-1'
#activation-details.d-none
  = render 'conductor/home/activation_details'