.row
  .col-sm-12
    ol.breadcrumb.text-right
      li
        a href="#" Dashboard
      li
        a href="#" #{@company.name}
      li
        a href="#" Manpower
      li.active Edit
    h1.page-header Edit Availability
.container-fluid
  .row
    .col-sm-2.col-xs-2.nopadding
      .panel.panel-default.simple-calendar
        .panel-heading.calendar-heading
          span.calendar-title
            | &nbsp;
        table.table.table-striped
          thead
            tr
              th Time
          tbody
            tr
              td.day.text-nowrap
                br
                p All day
                - @times_header.each do |time|
                  hr
                  p #{time}
    .col-sm-10.col-xs-10.nopadding
      = form_tag conductor_availability_path(@availability.id), method: :patch do
        = render 'add_avaibilities'