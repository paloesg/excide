.row
  .col.s12.m4.l4.hide-on-small-only
    .row
      .col.s12
        .card
          .card-image#profile-image
            = @company.image_url.present? ? image_tag(@company.image_url) : image_tag('https://placeholdit.imgix.net/~text?txtsize=32&txt=No+profile+image&w=300&h=300&txttrack=0')
            .card-title.truncate = @company.name
          .card-content
            p = @company.industry
            p = @company.company_type.try(:humanize)
      .col.s12
        = link_to "Post New Project", new_company_project_path, class: 'btn green darken-3 wide-button btn-large'
    .row
      .col.s12
        = link_to "Edit Company Details", edit_company_path, class: 'btn green darken-3 wide-button btn-large'
  .col.s12.m8.l8
    h5 My Projects
    - if @projects.present?
      - @projects.each do |p|
        .row
          .col.s12
            .card
              .card-header = p.title
              .card-content
                .row
                  .col.s12
                    .chip = p.project_category.try(:name)
                .row
                  .col.s12
                    p = p.description.html_safe
                    br
                    p
                      strong Remarks:
                      br
                      = p.remarks
                    br
                    .left
                      strong
                        ' Start Date:
                      = p.start_date.strftime('%b %Y')
                    .right
                      strong
                        ' End Date:
                      = p.end_date.strftime('%b %Y')
              .card-action
                = p.status.humanize.upcase
                .right
                  = link_to('View Proposals', company_project_path(p.id))
                  = link_to('Edit', edit_company_project_path(p.id))
    - else
      p = link_to('Post a project', new_company_project_path)