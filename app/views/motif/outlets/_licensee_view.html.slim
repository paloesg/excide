table.table.ml-4
  thead
    tr
      th.text-muted Franchise Licensee
      th.text-muted Outlet Name
      th.text-muted Expire on
      th 
  tbody
    - @outlets.each do |outlet|
      tr
        td
          = link_to outlet&.franchise_licensee || "Add license name ", edit_motif_outlet_path(outlet.id), class: "text-primary"
        td.font-weight-boldest 
          = link_to outlet.name, edit_motif_outlet_path(outlet.id), class: "text-dark"
        td = outlet&.expiry_date&.strftime("%d %b %Y") || "No expiry date stated"
        td
