// Check whether modal has been rendered before
if ($("#documentModal_" + "<%= @document.id %>").length == 0) {
  $('#showDocument').append('<%= j render partial: "motif/shared/show_document", locals: { d: @document } %>');
}
$('#documentModal_' + '<%= @document.id %>').modal('show');
