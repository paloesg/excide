.table-responsive
  table.table
    thead
      tr
        th.pb-2 style="width: 5%"
          = link_to_add_steps '+', f, :tasks, nil, class: 'text-muted add-template'
        th style="width: 70%" 
          span.mr-5.my-0 Tasks
        th style="width: 15%" Days to complete
        th style="width: 5%"
        th style="width: 5%" 
    tbody.motif-tasks
      #tasksOffcanvas
      = f.fields_for :tasks do |task|
        = render 'task_fields', f: task
/ Only render edit drawer if section ID is present. This is to check if form comes from edit templates or new templates
- if f.object.id.present?
  = render 'edit_drawer', f: f
.d-none#task_drawer_base
  = render 'new_drawer', f: f

