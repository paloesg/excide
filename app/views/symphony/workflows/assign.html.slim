.row
  .col-sm-12
    ol.breadcrumb
      li.breadcrumb-item.ml-auto
        a href="#" Home
      li.breadcrumb-item
        a href="#" Company
      li.breadcrumb-item
        a href="#" #{@template.title}
      li.breadcrumb-item.active #{@workflow.identifier}
    h1 User Assignment
.row
  .col-md-8.col-sm-6
    - @sections.each do |section|
      .card.mb-3
        .card-header
          = section.display_name
        .card-body
          table.table
            thead
              tr
                th No.
                th Instructions
                th Role
                th User
                th
            tbody
              - section.tasks.each do |t|
                - action = t.get_workflow_action(@company.id, @workflow.identifier)
                tr
                  td = t.position.to_s + '.'
                  td
                    = t.instructions
                  td = t.role&.name&.humanize
                  td = form_for action, remote: true do |f|
                    = f.collection_select(:assigned_user_id, (User.with_role t.role&.name, @company), 'id', 'full_name', options = {include_blank: 'Select user...'}, html_options = {data: {action_id: action.id}, class: 'selectize', onchange: 'assign(this)'})
                  td
                    i.fa.fa-check.text-success style="opacity: 0;"
                    i.fa.fa-times.text-danger style="opacity: 0;"
  .col-md-4.col-sm-6
    = link_to "Finish Assignment", symphony_workflow_path(@template.slug, @workflow.identifier), class: 'btn btn-primary btn-block'
    br
    - @workflow.get_roles.each do |role|
      .card.mb-3
        .card-header
          = role.name.titleize
        .card-body
          - (User.with_role role.name, @company).each do |user|
            span.fa.fa-user aria-hidden="true"
            |  #{user.full_name}
            br
