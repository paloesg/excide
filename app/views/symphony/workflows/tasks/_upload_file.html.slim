.btn.btn-outline-secondary.btn-sm style=(action.completed? ? "pointer-events: none" : "")
  i.material-icons-outlined class=(action.completed? ? "text-muted" : "text-primary") upload
  = link_to "Upload File", new_symphony_document_path(workflow: workflow.id, workflow_action: action.id, upload_type: 'file-upload-task'), role: 'button', style: (action.completed? ? "pointer-events: none" : ""), class: (action.completed? ? "text-muted" : "text-primary")