- if action.completed
  = render "symphony/batches/tasks/completed"
- else
  = link_to 'Start', new_symphony_document_path(workflow: workflow.id), role: 'button', target: "_blank", class: 'btn btn-primary btn-sm mb-2'