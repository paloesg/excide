= render(partial: 'header')
  row
    columns
      p Hi #{@user.first_name},

      p
        | Please note that you have been unassigned from the following event:
        | #{@event.client.name} #{@event.start_time.strftime("%F")} #{@event.start_time.strftime("%H:%M")} - #{@event.end_time.strftime("%H:%M")}.